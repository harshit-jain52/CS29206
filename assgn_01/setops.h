#include <settype.h>

void setprint(numset);
int setsize(numset);
numset setaddelt(numset, int);
numset setdelelt(numset, int);
numset setunion(numset, numset);
numset setintersection(numset, numset);